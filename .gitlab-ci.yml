
pages:
  image: node:18
  script:
    - npm ci
    - npm run build
    - mkdir .public
    - cp -r build/* .public/
    - mv .public public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"